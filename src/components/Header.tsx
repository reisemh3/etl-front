import React from 'react';

const Header = () => {
    return (
        <header>
            <nav>
                {/* Contenu de votre en-tête de navigation */}
            </nav>
        </header>
    );
};

export default Header;
